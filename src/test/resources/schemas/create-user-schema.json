{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Schema for POST /api/users — validates user creation response",
  "type": "object",
  "required": ["name", "job", "id", "createdAt"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The name sent in the request, echoed back"
    },
    "job": {
      "type": "string",
      "description": "The job sent in the request, echoed back"
    },
    "id": {
      "type": "string",
      "description": "Server-generated ID (returned as string by Reqres)"
    },
    "createdAt": {
      "type": "string",
      "description": "ISO 8601 timestamp of when the user was created"
    }
  }
}